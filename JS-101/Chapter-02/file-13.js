// Arrow function

// Simple sum function
let sum = (a, b) => a + b;
sum(2, 3);

// Single argument
let double = (n) => n * 2;

// No argument
let sayHi = () => "HELLO";

// Multilien arrow function
let add = (a, b) => {
  return a + b;
};
