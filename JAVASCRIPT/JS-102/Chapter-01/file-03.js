// document.getElementById("#root-id")
// The method getElementById can be called on document object.
const elem = document.getElementById("elem");
elem.style.background = "red";

// querySelectorAll
// It returns all the elements inside matching css selector.

// querySelector
// Returns first element

// elem.closest(css)
// It looks up for nearest parent

// getElementBy*
// elem.getElementByTagName(tag)
// elem.getElementByClassName(class)
// elem.getElementByName(name)

// All methods "getElementBy*" returns live collection

// querySelectorAll returns a static collection.
