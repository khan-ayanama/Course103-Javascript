// We can use <script> tag to add JS code into a page.
// The type and language attribute are not required anymore.
// <script type="text/javascript"/>

// Script to an external file can be inserted
// A single <script> tag can't have both the src attribute and code inside
// <script src="file.js">alert('hello')</script>

// Alert function

// Statements
alert("Hello");

// Semicolons
alert("Hello");

// Where Semiconlon is needed in js
alert("HELLO");
[1, 2].forEach(alert);

// "use strict"
// When use strict at the top of code it means modern js
// You can also use "use strict" at the beginning of the function
function myFunc() {
  "use strict";
  // function operations
}
